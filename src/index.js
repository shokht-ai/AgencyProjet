import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import AdminPackageDetailForm from './AdminPackageDetailForm';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AdminPackageDetailForm   />
  </React.StrictMode>
);
